# Submission artifact (regenerated by the runner)
submission.csv

# Local development split labels and private leaderboard history
dev_splits/

# Local experiment and SLURM outputs
logs/*
!logs/slurm/
logs/slurm/*
!logs/slurm/.gitkeep
wandb/
results/blackbox/*
!results/blackbox/leaderboard.md

# Local runner config (kept out of version control)
leaderboard/runner.yaml

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.uv-cache/
.ipynb_checkpoints/

# OS
.DS_Store
.env

# Probe weights (regenerated from activations; large binary blobs)
*.pt
!submission/whitebox_probe/**/*.pt

# sklearn serialized models
*.joblib
*.pkl

results/whitebox/*
# Contrastive extraction: 4 GB of .npz activations and regenerable weights.
# The small eval reports (*.json) stay tracked -- they are the evidence.
# Experiment run logs and smoke artifacts (not evidence)
experiments/**/*.log
experiments/**/*.smoke.json

results/contrastive/activations/
results/contrastive/weights/
dump/

# Local Lambda Cloud connection state (known hosts and campaign metadata)
.lambda/
