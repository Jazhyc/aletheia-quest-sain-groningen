# @package _global_
defaults:
  - pid_liars_bench_broad_aug_v1
  - _self_

method: qwen9b_pid_liars_broad_replay_continue_adamw2e5_v1

student:
  init_adapter: results/blackbox/qwen9b_privileged_gptoss120b_summary_variedonly_adamwlr5e5_v1/adapter
  output_dir: ${output_dir}/adapter
  training:
    optimizer: adamw
    optim: adamw_torch
    learning_rate: 2.0e-5
    num_train_epochs: 1.0
