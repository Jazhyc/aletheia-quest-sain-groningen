defaults:
  - pid_qwen27_tvg_binary_soft_distillation_v1
  - _self_

method: qwen9b_qwen397_tvg_binary_softonly_varied_v1
output_dir: results/blackbox/${method}

teacher:
  artifact: results/blackbox/qwen35_397b_fp8_nothink_truth_value_binary_logit_v1/train/student_rows.jsonl

student:
  model_loader: image_text_to_text
  soft_teacher_artifact: results/blackbox/qwen35_397b_fp8_nothink_truth_value_binary_logit_v1/train/soft_targets.jsonl
  output_dir: ${output_dir}/adapter
  lora:
    exclude_modules: ".*(visual|vision_tower|merger|patch_embed).*"
