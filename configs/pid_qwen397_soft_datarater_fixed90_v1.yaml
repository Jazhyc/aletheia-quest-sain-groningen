# @package _global_
# Shared fixed-update recipe for Qwen-397B soft-DataRater comparisons.

defaults:
  - pid_qwen397_tvg_binary_soft_distillation_v1
  - _self_

method: qwen9b_qwen397_soft_datarater_fixed90_v1
output_dir: results/blackbox/${method}

student:
  selection_manifest: null
  output_dir: ${output_dir}/adapter
  training:
    # Match the full-data baseline's 90 updates after filtering.
    max_steps: 90
