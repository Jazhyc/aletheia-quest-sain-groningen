# One-example-per-label calibration for the current highest validation prompt.

defaults:
  - blackbox_reasoning_known4096
  - _self_

method: qwen_reason_known_shot1_4096_v1

shots:
  n_per_label: 1
  split: train
  seed: 0
  max_prompt_chars: 1800
